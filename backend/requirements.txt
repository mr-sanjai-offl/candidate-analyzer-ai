# ==============================================================================
# ApexGuidance AI — Python Dependencies
# Architecture Bible Section 5: requirements.txt
# ==============================================================================

# --- Core Framework ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0

# --- Database (Architecture Section 4: SQLAlchemy 2.0, Supabase PostgreSQL) ---
sqlalchemy[asyncio]>=2.0.35,<3.0.0
asyncpg>=0.30.0,<1.0.0
alembic>=1.13.0,<2.0.0

# --- Redis & Celery (Architecture Section 4) ---
redis>=5.1.0,<6.0.0
celery>=5.4.0,<6.0.0

# --- Async HTTP Client (Architecture Section 7: no synchronous HTTP) ---
httpx>=0.27.0,<1.0.0

# --- Storage & Parsing (Phase 5 & 9) ---
supabase>=2.3.0,<3.0.0
PyMuPDF>=1.23.0,<2.0.0
pdfplumber>=0.10.0,<1.0.0
python-docx>=1.1.0,<2.0.0
pytesseract>=0.3.10,<1.0.0
python-magic>=0.4.27,<1.0.0
python-magic-bin>=0.4.14,<1.0.0; sys_platform == 'win32'

# --- Monitoring (Architecture Section 4: Sentry) ---
sentry-sdk[fastapi]>=2.14.0,<3.0.0

# --- Security (Architecture Section 10) ---
bcrypt>=4.1.0,<5.0.0

# --- Testing (Architecture Section 11) ---
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0

# --- Code Quality ---
ruff>=0.6.0
black>=24.8.0,<26.0.0
mypy>=1.11.0,<2.0.0
pre-commit>=3.8.0,<4.0.0
